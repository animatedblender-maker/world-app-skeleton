# Render / nested monorepo: do not use the parent workspace lockfile.
# Without this, `npm ci` in apps/api can resolve against the repo-root lock
# (which has no @aws-sdk/*) and fail with "Missing from lock file".
workspaces=false
install-strategy=hoisted
