{
  "name": "mobile",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Native only: Swift iOS (ios/WorldApp) + Kotlin Android (android-app). No Capacitor.",
  "scripts": {
    "ios:open": "open ios/WorldApp/WorldApp.xcodeproj",
    "android:open": "echo Open apps/mobile/android-app in Android Studio",
    "android:run": "cd android-app && ./gradlew :app:installDebug"
  }
}
