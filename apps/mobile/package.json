{
  "name": "mobile",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Native only: Swift iOS (ios/WorldApp) + Kotlin Android (android-app). No Capacitor.",
  "scripts": {
    "ios:open": "open ios/WorldApp/WorldApp.xcodeproj",
    "android:open": "open -a 'Android Studio' android-app",
    "android:run": "cd android-app && ./gradlew :app:installDebug && adb shell am start -n com.matterya.matterya/.MainActivity"
  }
}
